# nixpacks.yaml

setup:
  - python3
  - gcc

install:
  - python -m venv --copies /opt/venv
  - . /opt/venv/bin/activate
  - pip install -r requirements.txt

start:
  - . /opt/venv/bin/activate && 1.py  # 假设您的 Flask 应用程序文件名为 your_app.py
